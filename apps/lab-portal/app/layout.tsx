import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Health Ocean Lab Portal - Laboratory Management',
  description: 'Laboratory management portal for Health Ocean diagnostic centers',
  keywords: ['lab portal', 'laboratory', 'diagnostics', 'test management'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
