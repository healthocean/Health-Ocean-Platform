{
  "name": "@health-ocean/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Health Ocean",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}